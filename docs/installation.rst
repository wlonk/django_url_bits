.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install django_url_bits

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_url_bits
    $ pip install django_url_bits
